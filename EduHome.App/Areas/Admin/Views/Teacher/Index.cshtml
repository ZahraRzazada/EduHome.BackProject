@model IEnumerable<TeacherGetDto>
<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">
                        <a class="btn btn-success" asp-action="create">
                            Add New Teacher
                        </a>
                    </h4>
                    <p class="card-description">
                        Add class <code>.table</code>
                    </p>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>FullName</th>
                                    <th>Position</th>
                                    <th>CreatedAt</th>
                                    <th>Operations</th>
                             
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (TeacherGetDto item in Model)
                                {
                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.FullName </td>
                                        <td>@item.Position.Name</td>
                                        <td>@item.CreatedAt.ToString("dd.MM.yyyy")</td>
                                        <td><a onclick="return confirm('are you sure?')" asp-action="remove" asp-route-id="@item.Id" class="badge badge-danger">Remove</a> <a asp-action="update" asp-route-id="@item.Id" class="badge badge-warning">Update</a></td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

